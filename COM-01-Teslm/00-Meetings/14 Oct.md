#### Done
1. Deployed `teslm-admin` (dashboard) on s3 buckets. then distributed it with cloud run All this use CI/CD pipeline. 
2. Create buckup and restore script form dev database which use s3 as a destination bacup 
3. Create `Teslm Terraform modules` which will be the base modules for all projects in GCP. 
4. Demo of applying Teslm app on `Cloud Run` with terraform
#### To Do
- Deploy Elastic Search and create Kibana Dashboard for log search. 
- Apply GCP startup program 
- Create Secret Manager module for backend